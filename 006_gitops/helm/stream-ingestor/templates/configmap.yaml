apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stream-ingestor.fullname" . }}-config
  labels:
    {{- include "stream-ingestor.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
