# Environment-specific values for order-service — staging environment.
# This file is layered ON TOP of values.yaml by ArgoCD:
#   helm.valueFiles: [values.yaml, values-staging.yaml]
# Only values that differ from defaults are listed here.

image:
  repository: nexus.company.internal/kube-demo/order-service
  tag: "staging-latest"

replicaCount: 1

autoscaling:
  enabled: false
