apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "risk-engine.fullname" . }}
  labels:
    {{- include "risk-engine.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.config | nindent 2 }}
