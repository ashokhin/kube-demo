apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "risk-engine.fullname" . }}
  labels:
    {{- include "risk-engine.labels" . | nindent 4 }}
# risk-engine does not call the Kubernetes API — no token mount needed.
automountServiceAccountToken: false
