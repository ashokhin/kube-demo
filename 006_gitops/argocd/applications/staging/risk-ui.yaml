---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: risk-ui-staging
  namespace: argocd
spec:
  project: hadoop-demo
  source:
    repoURL: "git@bitbucket.company.internal:devops/hadoop-demo-gitops.git"
    targetRevision: HEAD
    path: helm/risk-ui
    helm:
      valueFiles:
        - values.yaml
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: hadoop-demo-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: false   # staging: no auto self-heal to preserve manual changes during QA
    syncOptions:
      - CreateNamespace=true
