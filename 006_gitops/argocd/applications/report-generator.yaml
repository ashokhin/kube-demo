apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: report-generator
  namespace: argocd
  annotations:
    # Wave 3: last, after all services are up.
    # The CronJob itself does nothing until its schedule fires —
    # deploying it last is a convention, not a strict requirement.
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: kube-demo
  source:
    repoURL: https://github.com/ashokhin/gitops.git
    targetRevision: HEAD
    path: helm/report-generator
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
