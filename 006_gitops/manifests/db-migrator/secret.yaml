# SOURCE: helm/db-migrator/templates/secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: kube-demo-db-migrator
  namespace: kube-demo
  labels:
    helm.sh/chart: db-migrator-0.1.0
    app.kubernetes.io/name: db-migrator
    app.kubernetes.io/instance: kube-demo
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
type: Opaque
stringData:
  # Sync driver (psycopg2), not async — Alembic runs synchronously.
  DATABASE_URL: "postgresql://user:password@kube-demo-postgresql:5432/orders"
