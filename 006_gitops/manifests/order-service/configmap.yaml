# SOURCE: helm/order-service/templates/configmap.yaml
# GENERATED FROM: .Values.config.*

apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-demo-order-service
  namespace: kube-demo
  labels:
    helm.sh/chart: order-service-0.1.0
    app.kubernetes.io/name: order-service
    app.kubernetes.io/instance: kube-demo
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
data:
  LOG_LEVEL: "INFO"
  PORT: "8080"
  # RabbitMQ exchange name — not sensitive, so it belongs in ConfigMap, not Secret.
  ORDERS_EXCHANGE: "orders"
